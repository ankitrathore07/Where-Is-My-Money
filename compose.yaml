name: where-is-my-money

services:
  web:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
    volumes:
      - app_data:/app/data

volumes:
  app_data:
